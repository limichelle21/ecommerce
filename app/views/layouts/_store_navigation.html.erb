<nav class="navbar navbar-default" role="navigation">
	<div class="navbar-collapse collapse">
		<ul class="nav navbar-nav navbar-left">
			
			<% if current_store.present? %>
				<li><%= link_to 'Home', store_path(current_store.id) %></li>
		 		<li><%= link_to "Shop Now", store_products_path(current_store.id) %></li>
		 	<% else %>
		 		<li><%= link_to 'Home', root_path %></li>
		 	<% end %>
		 
		</ul>
		<ul class="nav navbar-nav navbar-right">
	  		<% if current_customer.present? %>
	  			<li><%= image_tag current_customer.avatar_url(36), class: "gravatar" %></li>
		  		<li><%= link_to current_customer.email %></li>
		  		<li><%= link_to "Sign Out", destroy_customer_session_path, method: :delete %></li>
	  		<% else %>
		  		<li><%= link_to 'Sign Up', new_customer_registration_path %></li> 
		  		<li><%= link_to 'Sign In', new_customer_session_path %></li>
		 	<% end %>
		 	<% if current_order.present? %>
			 	<li class="cart-text"><%= render partial: "layouts/cart_text" %></li>
			<% end %>
		</ul>
	</div>
</nav>

