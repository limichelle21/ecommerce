<nav class="navbar navbar-default">
	<div class="container">
		<div class="navbar-header">
			<ul class="nav nav-tabs" role="navigation">
				<li><%= link_to 'Home', root_path %></li>
				
				<form class="navbar-form navbar-right" role="search">
					<div class="form-group">
						<input type="text" class="form-control" placeholder="Search Store">
					</div>
					<button type="submit" class="btn btn-default">Search</button>
				</form>
				<div class="pull-right user-info">
			  		<% if current_user %>
				  		<div class="pull-right">
				  			<%= link_to current_user.name, user_session_path(current_user) %> <br/> <%= link_to "Sign Out", destroy_user_session_path(current_user), method: :delete %>
				  		</div>
			  		<% else %>
				  		<%= link_to 'Sign Up', new_user_registration_path %> or 
				  		<%= link_to 'Sign In', user_session_path %>
				 	<% end %>
				 	<button type="button"><span class="glyphicon glyphicon-shopping-cart"></span></button>
			  </div>
			</ul>
		</div>
	</div>
</nav>


